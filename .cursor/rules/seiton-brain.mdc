---
description: Seiton Brain project context — architecture, status, conventions
alwaysApply: true
---

# Seiton Brain

Personal AI second brain: capture (Telegram/API) → classify → Obsidian vault;
retrieve (search, RAG `/ask`, digest `/digest`, REST, MCP).

**Produkt:** Öffentliche Entwicklung (MIT). Geplante kommerzielle
Consumer-Edition — [ADR 0004](docs/adr/0004-commercial-consumer-product.md),
[ADR 0005](docs/adr/0005-repo-and-license-strategy.md),
[ADR 0006](docs/adr/0006-consumer-stack-no-sqlite-fork.md) (ein Stack). Name from **Seiton** (整頓).

**Deployment (normativ, [ADR 0008](docs/adr/0008-deployment-models-self-hosted-first.md)):**
Self-hosting ist ein Deployment-Modell, nicht die Produktidentität. Zuerst
self-hosted; **Managed Cloud** ist Teil der Vision, aber später (Phase I / E24,
gated auf ADR 0007) und **kein V1-Blocker**. Product Core bleibt deployment-neutral
— keine Cloud-Abstraktionen, kein Multi-Tenant-Datenmodell, kein Billing auf
Vorrat. Isolationsgrenze = die Instanz (heutiger Horizont, nicht irreversibel).
Absolute Aussagen wie „wir betreiben nichts" / „SaaS verworfen" aus ADR 0004
sind aufgehoben.

**Aktueller Stand (lesen zuerst):** [`docs/current-state.md`](docs/current-state.md) —
nicht die gesamte historische Roadmap.

## Flow

```
Telegram / API → Worker: [Whisper if voice] → classify → Postgres → .md in vault
              → optional: /ask, /digest (RAG), webhooks, MCP via REST
Web-UI (localhost): /setup → /dashboard, /ask, /notes, /settings
```

## Stack

FastAPI · PostgreSQL + SQLAlchemy (async) + Alembic · Celery + Redis · OpenAI (LLM + Whisper + Embeddings) · pgvector · Jinja2 · Docker Compose · Obsidian vault

## Structure

- `app/telegram/` — webhook, polling, slash commands
- `app/worker/` — Celery tasks (capture, voice, ask, digest)
- `app/llm/` — provider, embeddings, schemas
- `app/services/` — process_message, answer, digest
- `app/vault/` — backend (Protocol), filesystem, reader, writer, index (keyword + semantic search)
- `app/api/v1/` — REST API (capture, ask, digest, search, …)
- `app/setup/` — Setup-Wizard (env checks, `.env` schreiben)
- `app/licensing/` — Offline-Lizenz Ed25519 (E21-1), `docs/licensing.md`
- `app/ui/` — Web-UI: templates, static, dashboard/ask API (`/api/ui/*`)
- `app/webhooks/` — outbound events (note.created, note.indexed, …)
- `examples/mcp/` — MCP server for Cursor/Claude
- `examples/n8n/` — Power-user workflow JSONs (no custom node)
- `prompts/` — versioned LLM prompts

## Status

v0.3.0. Phasen A–H Kern 🟢. **Aktiv: Phase L** (Launch-Härtung) + E45 Engineering.
Details und nächstes Paket: [`docs/current-state.md`](docs/current-state.md) und
[`ROADMAP.md`](ROADMAP.md). Erledigte Epics E1–E26:
[`docs/archive/roadmap-phases-a-h.md`](docs/archive/roadmap-phases-a-h.md).
Phasen M–O Detail: [`docs/roadmap-phases-m-o.md`](docs/roadmap-phases-m-o.md).

## Roadmap & Backlog

- **`docs/current-state.md`** — Kurzstand (Agent-Einstieg)
- **`ROADMAP.md`** — aktive Planung (≲ ~400 Zeilen)
- **`docs/archive/`** — abgeschlossene Roadmap-Historie
- **`ARCHITECTURE.md`** — Diagramm, Modul-Map
- **`CHANGELOG.md`** — Keep-a-Changelog
- **`docs/adr/`** — ADRs (0001–0008; **0008 = Deployment-Modelle, normativ**;
  0007 = Cloud-Abo/Betrieb, Proposed; 0004 dadurch teilweise präzisiert)
- **`docs/engineering.md`** — Solo-Dev-Workflow (E45)
- **`docs/production-ops.md`** — Production Ops (E46)
- **`docs/integrations/`** — n8n (REST-only), MCP, knowledge-retrieval

Bei neuen Features: zuerst ROADMAP-Eintrag und/oder Issue, dann Code.
Vor größeren Architektur-Änderungen: `ARCHITECTURE.md` + relevante ADRs lesen (E45-2).
UI: Designsystem [`docs/design-system.md`](../docs/design-system.md) (E47-3) befolgen;
Referenzen [`docs/ui-reference-request.md`](../docs/ui-reference-request.md);
Ist-Stand [`docs/ui-inventory.md`](../docs/ui-inventory.md); Rule `.cursor/rules/ui-design-system.mdc`.

## Conventions

- Secrets in `.env` only; see `.env.example`
- `.gitignore`: `/vault/` and `/models/` (root only) — never `app/vault/` or `app/models/` (ADR 0002)
- Celery tasks use `worker_session()` (ADR 0001)
- Alembic migrations under `alembic/versions/`
- User prefers German; README DE+EN; Roadmap/ADRs auf Deutsch
- Eine Story → eine PR; CHANGELOG unter `[Unreleased]`
- **Kein Custom-n8n-Node** (ADR 0004); REST + `examples/n8n/` für Power-User

## Key files

- `docker-compose.yml` — api, worker, db, redis; `docker-compose.consumer.yml` (E20-1), `.vps.yml` (E20-2)
- `scripts/install.sh`, `scripts/init.sh`, `scripts/deploy-vps.sh`, `scripts/update.sh`, `scripts/doctor.sh`
- `app/main.py` — health, API + UI mount
- `app/db/session.py` — `get_db()`, `worker_session()`
- `app/ui/router.py` — `/dashboard`, `/ask`, `/notes`, `/settings`, `/setup`, `/api/ui/*`
